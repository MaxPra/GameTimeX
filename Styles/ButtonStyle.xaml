<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Moderner Primärbutton -->
    <Style x:Key="PrimaryButton" TargetType="Button">
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="OverridesDefaultStyle" Value="True"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Padding" Value="16,10"/>
        <Setter Property="MinHeight" Value="38"/>
        <Setter Property="Background" Value="{StaticResource ButtonDefaultBrush}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Cursor" Value="Arrow"/>
        <!-- Fokus-Visual komplett abschalten (keine graue Umrandung) -->
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Grid x:Name="Root" RenderTransformOrigin="0.5,0.5">
                        <Grid.RenderTransform>
                            <ScaleTransform ScaleX="1" ScaleY="1"/>
                        </Grid.RenderTransform>

                        <!-- weicher Schatten -->
                        <Border CornerRadius="10" Background="Transparent">
                            <Border.Effect>
                                <DropShadowEffect Color="#40000000"
                                              BlurRadius="10"
                                              ShadowDepth="1.5"
                                              Opacity="0.6"/>
                            </Border.Effect>
                        </Border>

                        <!-- Grundfläche -->
                        <Border x:Name="Base"
                            CornerRadius="10"
                            Background="{TemplateBinding Background}"/>

                        <!-- zarte Top-Glanzkante -->
                        <Border CornerRadius="10" IsHitTestVisible="False" Opacity="0.45">
                            <Border.OpacityMask>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                    <GradientStop Offset="0.00" Color="#66FFFFFF"/>
                                    <GradientStop Offset="0.10" Color="#22FFFFFF"/>
                                    <GradientStop Offset="0.25" Color="#00000000"/>
                                    <GradientStop Offset="1.00" Color="#00000000"/>
                                </LinearGradientBrush>
                            </Border.OpacityMask>
                            <Border.Background>#FF000000</Border.Background>
                        </Border>

                        <!-- feiner Innen-/Außenrahmen -->
                        <Border CornerRadius="10" BorderBrush="#22000000" BorderThickness="1" Background="Transparent"/>
                        <Border CornerRadius="9"  BorderBrush="#22FFFFFF" BorderThickness="1" Background="Transparent" Margin="1"/>

                        <!-- Inhalt -->
                        <ContentPresenter Margin="10,0"
                                      VerticalAlignment="Center"
                                      HorizontalAlignment="Center"/>
                    </Grid>

                    <!-- Zustände -->
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Base" Property="Background" Value="{StaticResource ButtonHoverBrush}"/>
                        </Trigger>

                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Base" Property="Background" Value="{StaticResource ButtonPressedBrush}"/>
                            <Setter TargetName="Root" Property="RenderTransform">
                                <Setter.Value>
                                    <ScaleTransform ScaleX="0.985" ScaleY="0.985"/>
                                </Setter.Value>
                            </Setter>
                        </Trigger>

                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Base" Property="Opacity" Value="0.55"/>
                            <Setter Property="Foreground" Value="#88FFFFFF"/>
                            <Setter Property="Cursor" Value="Arrow"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Für Kompatibilität: alter Schlüssel nutzt jetzt den neuen Look -->
    <Style x:Key="NormalGuiButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}"/>

    <SolidColorBrush x:Key="StrokeBrush" Color="#26FFFFFF"/>

    <Style x:Key="EditCircleIconButton" TargetType="Button">
        <Setter Property="Width" Value="32"/>
        <Setter Property="Height" Value="32"/>
        <Setter Property="ToolTip" Value="Rename"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Grid>
                        <!-- runder Hintergrund -->
                        <Border x:Name="circle"
                            CornerRadius="16"
                            Background="#1AFFFFFF"
                            BorderBrush="{StaticResource StrokeBrush}"
                            BorderThickness="1"/>
                        <!-- dicker Pencil aus MDL2 -->
                        <TextBlock Text="&#xE70F;"                 
                               FontFamily="Segoe MDL2 Assets"
                               FontSize="18"
                               Foreground="White"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"/>
                </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="circle" Property="Background" Value="#28FFFFFF"/>
                            <Setter TargetName="circle" Property="BorderBrush" Value="{DynamicResource ButtonDefaultBrush}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="circle" Property="Background" Value="#40FFFFFF"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="circle" Property="Opacity" Value="0.6"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>





    <!-- Danger/Cancel Button (wie PrimaryButton, aber rot) -->
    <Style x:Key="CancelButton" TargetType="Button">
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="OverridesDefaultStyle" Value="True"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Padding" Value="16,10"/>
        <Setter Property="MinHeight" Value="38"/>
        <Setter Property="Background" Value="{StaticResource ButtonCancelBrush}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Cursor" Value="Arrow"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Grid x:Name="Root">
                        <!-- Schatten -->
                        <Border CornerRadius="10" Background="Transparent">
                            <Border.Effect>
                                <DropShadowEffect Color="#40000000" BlurRadius="10" ShadowDepth="1.5" Opacity="0.6"/>
                            </Border.Effect>
                        </Border>

                        <!-- Grundfläche -->
                        <Border x:Name="Base" CornerRadius="10"
                            Background="{TemplateBinding Background}"/>

                        <!-- Glanzkante oben -->
                        <Border CornerRadius="10" IsHitTestVisible="False" Opacity="0.45">
                            <Border.OpacityMask>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                    <GradientStop Offset="0.00" Color="#66FFFFFF"/>
                                    <GradientStop Offset="0.10" Color="#22FFFFFF"/>
                                    <GradientStop Offset="0.25" Color="#00000000"/>
                                    <GradientStop Offset="1.00" Color="#00000000"/>
                                </LinearGradientBrush>
                            </Border.OpacityMask>
                            <Border.Background>#FF000000</Border.Background>
                        </Border>

                        <!-- feine Rahmen -->
                        <Border CornerRadius="10" BorderBrush="#22000000" BorderThickness="1" Background="Transparent"/>
                        <Border CornerRadius="9" BorderBrush="#22FFFFFF" BorderThickness="1" Background="Transparent" Margin="1"/>

                        <!-- Focus-Ring -->
                        <Border x:Name="FocusRing" CornerRadius="12"
                            BorderBrush="#66FFFFFF" BorderThickness="2"
                            Opacity="0" Margin="-2"/>

                        <ContentPresenter Margin="10,0"
                                      VerticalAlignment="Center"
                                      HorizontalAlignment="Center"/>
                    </Grid>

                    <!-- Zustände -->
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Base" Property="Background" Value="{StaticResource ButtonCancelHoverBrush}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Base" Property="Background" Value="{StaticResource ButtonCancelPressedBrush}"/>
                            <Setter TargetName="Root" Property="RenderTransform">
                                <Setter.Value>
                                    <ScaleTransform ScaleX="0.985" ScaleY="0.985"/>
                                </Setter.Value>
                            </Setter>
                            <Setter TargetName="Root" Property="RenderTransformOrigin" Value="0.5,0.5"/>
                        </Trigger>
                        <Trigger Property="IsKeyboardFocused" Value="True">
                            <Setter TargetName="FocusRing" Property="Opacity" Value="1"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Base" Property="Opacity" Value="0.55"/>
                            <Setter Property="Foreground" Value="#88FFFFFF"/>
                            <Setter Property="Cursor" Value="Arrow"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>


    <!-- Start/Stop Monitoring Button -->
    <Style x:Key="StartStopMonitoringButton" TargetType="Button">
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="OverridesDefaultStyle" Value="True"/>
        <Setter Property="Foreground" Value="#FFFFFFFF"/>
        <Setter Property="FontSize" Value="15"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Padding" Value="18,10"/>
        <Setter Property="MinHeight" Value="40"/>
        <Setter Property="Cursor" Value="Arrow"/>
        
        <Setter Property="Background" Value="{DynamicResource ButtonDefaultBrush}"/>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Grid>
                        <!-- Weicher DropShadow wie bei Steam-Karten -->
                        <Border CornerRadius="12" Background="Transparent">
                            <Border.Effect>
                                <DropShadowEffect Color="#40000000"
                                              BlurRadius="10"
                                              ShadowDepth="1.5"
                                              Opacity="0.65"/>
                            </Border.Effect>
                        </Border>

                        <Border x:Name="Base"
                            CornerRadius="12"
                            Background="{TemplateBinding Background}"/>

                        <Border CornerRadius="12">
                            <Border.OpacityMask>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                    <GradientStop Offset="0.00" Color="#00000000"/>
                                    <GradientStop Offset="0.60" Color="#00000000"/>
                                    <GradientStop Offset="1.00" Color="#28000000"/>
                                </LinearGradientBrush>
                            </Border.OpacityMask>
                            <Border.Background>#FF000000</Border.Background>
                        </Border>

                        <Border CornerRadius="12" IsHitTestVisible="False" Margin="1" Opacity="0.45" Background="#FFFFFFFF">
                            <Border.OpacityMask>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                    <GradientStop Offset="0.00" Color="#66FFFFFF"/>
                                    <GradientStop Offset="0.08" Color="#22FFFFFF"/>
                                    <GradientStop Offset="0.18" Color="#00000000"/>
                                    <GradientStop Offset="1.00" Color="#00000000"/>
                                </LinearGradientBrush>
                            </Border.OpacityMask>
                        </Border>

                        <Border CornerRadius="12" IsHitTestVisible="False">
                            <Border.OpacityMask>
                                <RadialGradientBrush Center="0.5,0.5" RadiusX="0.9" RadiusY="0.9">
                                    <GradientStop Offset="0.80" Color="#00000000"/>
                                    <GradientStop Offset="1.00" Color="#33000000"/>
                                </RadialGradientBrush>
                            </Border.OpacityMask>
                            <Border.Background>#FF000000</Border.Background>
                        </Border>
                        
                        <Border CornerRadius="12" BorderThickness="1" BorderBrush="#33000000" Background="Transparent"/>
                        <Border CornerRadius="11" BorderThickness="1" BorderBrush="#22FFFFFF" Background="Transparent" Margin="1"/>

                        <!-- Inhalt: Play-Icon + Text -->
                        <Grid Margin="14,6">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="8"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <!-- Play-Icon (weiß) -->
                            <Path x:Name="Icon"
                              Grid.Column="0"
                              Width="12" Height="12"
                              Fill="{TemplateBinding Foreground}"
                              Stretch="Uniform"
                              Data="M 0,0 L 0,12 L 10,6 Z"
                              VerticalAlignment="Center"/>

                            <!-- Text -->
                            <ContentPresenter Grid.Column="2"
                                          VerticalAlignment="Center"
                                          HorizontalAlignment="Center"
                                          RecognizesAccessKey="True"/>
                        </Grid>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>








    <!-- Menu-Bar Button Style -->
    <Style x:Key="MenuBarButton" TargetType="{x:Type Button}">
        <Setter Property="SnapsToDevicePixels" Value="true" />
        <Setter Property="OverridesDefaultStyle" Value="true" />
        <Setter Property="BorderBrush" Value="LightGray"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Border x:Name="Border"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        Background="{TemplateBinding Background}"
                        CornerRadius="6">
                        <ContentPresenter Margin="2"
                                      HorizontalAlignment="Center"
                                      VerticalAlignment="Center"
                                      RecognizesAccessKey="True" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Trigger.EnterActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <ColorAnimation Storyboard.TargetProperty="Background.(SolidColorBrush.Color)"
                                        To="{StaticResource ButtonHoverColor}" Duration="0:0:0.2"/>
                        </Storyboard>
                    </BeginStoryboard>
                </Trigger.EnterActions>
                <Trigger.ExitActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <ColorAnimation Storyboard.TargetProperty="Background.(SolidColorBrush.Color)"
                                        To="Transparent" Duration="0:0:0.2"/>
                        </Storyboard>
                    </BeginStoryboard>
                </Trigger.ExitActions>
            </Trigger>
        </Style.Triggers>
    </Style>


    <Style x:Key="EditProfileNameButtonStyle" TargetType="{x:Type Button}">
        <Setter Property="SnapsToDevicePixels" Value="true" />
        <Setter Property="OverridesDefaultStyle" Value="true" />
        <Setter Property="BorderBrush" Value="LightGray"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Border x:Name="Border"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        Background="{TemplateBinding Background}"   >
                        <ContentPresenter Margin="2"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        RecognizesAccessKey="True" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Cursor" Value="Hand"/>
            </Trigger>
            <Trigger Property="IsMouseOver" Value="False">
                <Setter Property="Cursor" Value="None"/>
            </Trigger>
        </Style.Triggers>
    </Style>

</ResourceDictionary>